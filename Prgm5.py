# write a SQL query to find those employees whose salary is less than 6000.
# Return full name (first and last name), and salary.

#Answer:
# SELECT first_Name || ' ' ||last_name As full_name,salary FROM Employees WHERE salary>6000